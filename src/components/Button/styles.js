import { StyleSheet } from "react-native"

export const styles = StyleSheet.create({
    container: {
        backgroundColor: '#4F63AC',
        padding: 20,
        borderRadius: 10,
        margin: 30,
        width: '100%'
        
        
    },
    title: {
        color: 'cyan',
        textAlign: 'center',
        fontSize: 16,
        fontWeight: 'bold'
    }
})